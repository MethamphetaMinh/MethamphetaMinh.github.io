<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ha Tuan Minh</title>
    <style>
        /* CSS nội tuyến cơ bản cho màu Turquoise để dễ hình dung */
        :root {
            --turquoise: #40E0D0; /* Màu chủ đạo */
            --dark-bg: #1A1A1A; /* Nền tối */
            --light-text: #F7F7F7; /* Chữ sáng */
            --highlight-bg: #f0e68c; /* Màu vàng nhạt cho Studying (Giống mẫu) */
            --dark-text: #333; /* Màu chữ tối cho Studying */
        }
        
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--dark-bg);
            color: var(--light-text);
        }

        .navbar {
            background-color: var(--dark-bg);
            padding: 15px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 2px solid var(--turquoise);
        }

        .logo a {
            color: var(--turquoise);
            font-size: 1.5em;
            font-weight: bold;
            text-decoration: none;
        }

        .nav-links a {
            color: var(--light-text);
            text-decoration: none;
            margin-left: 20px;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: var(--turquoise);
        }

        .hero {
            text-align: center;
            padding: 100px 5%;
        }

        .hero h1 {
            font-size: 3em;
            margin-bottom: 10px;
        }

        .hero h1 span {
            color: var(--turquoise); /* Tên bạn/chức danh nổi bật */
        }

        .cta-button {
            display: inline-block;
            padding: 12px 30px;
            margin-top: 30px;
            background-color: var(--turquoise);
            color: var(--dark-bg);
            text-decoration: none;
            font-weight: bold;
            border-radius: 5px;
            transition: background-color 0.3s, transform 0.3s;
        }

        .cta-button:hover {
            background-color: #36c3b6; 
            transform: translateY(-2px);
        }

        section {
            padding: 60px 5%;
        }

        h2 {
            text-align: center;
            font-size: 2.5em;
            margin-bottom: 40px;
            color: var(--turquoise);
        }
        
        /* Cập nhật phần Giới thiệu để phù hợp với thông tin mới */
        #about {
            text-align: left; /* Thay đổi căn giữa thành căn trái */
            max-width: 900px;
            margin: 0 auto;
        }
        
        #about h2 {
            text-align: left; /* Tiêu đề trong phần About căn trái */
            color: var(--light-text); /* Đổi màu tiêu đề về màu chữ sáng */
            margin-bottom: 10px;
        }
        
        .personal-info a {
            color: var(--turquoise); /* Liên kết Email/Github màu Turquoise */
            text-decoration: none;
            margin-right: 15px;
        }
        
        .studying-tag {
            display: inline-block;
            background-color: var(--highlight-bg);
            color: var(--dark-text);
            padding: 10px 15px;
            border-radius: 5px;
            font-weight: bold;
            margin-right: 15px;
        }
        
        .research-section {
            margin-top: 30px;
            border-top: 1px solid #333;
            padding-top: 20px;
        }
        
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .project-card {
            background-color: #2a2a2a;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            border: 1px solid var(--turquoise);
        }

        .project-card img {
            width: 100%;
            height: auto;
        }

        .project-info {
            padding: 15px;
        }

        .project-info h3 {
            color: var(--turquoise);
            margin-top: 0;
        }
    </style>
</head>
<body>

    <header class="navbar">
        <div class="logo">
            <a href="#">[Tên Bạn]</a>
        </div>
        <nav class="nav-links">
            <a href="#projects">Dự án</a>
            <a href="#about">Giới thiệu</a>
            <a href="#skills">Kỹ năng</a>
            <a href="#contact">Liên hệ</a>
        </nav>
    </header>

    <section class="hero">
        <p>Xin chào, tôi là</p>
        <h1>[Tên Bạn], một <span class="highlight">Creative Designer & Developer</span></h1>
        <p>Tôi tạo ra trải nghiệm người dùng hiện đại và hiệu quả.</p>
        <a href="#projects" class="cta-button">Xem Công Việc Của Tôi</a>
    </section>

    <section id="projects">
        <h2>Dự Án Nổi Bật</h2>
        <div class="projects-grid">
            <div class="project-card">
                <img src="https://via.placeholder.com/350x200?text=Project+1" alt="Dự án 1">
                <div class="project-info">
                    <h3>Project Name 1</h3>
                    <p>Thiết kế UX/UI cho ứng dụng di động về quản lý tài chính.</p>
                    <a href="#" style="color: var(--turquoise); text-decoration: none;">Chi tiết &rarr;</a>
                </div>
            </div>
            <div class="project-card">
                <img src="https://via.placeholder.com/350x200?text=Project+2" alt="Dự án 2">
                <div class="project-info">
                    <h3>Project Name 2</h3>
                    <p>Phát triển trang web thương mại điện tử sử dụng React.</p>
                    <a href="#" style="color: var(--turquoise); text-decoration: none;">Chi tiết &rarr;</a>
                </div>
            </div>
        </div>
    </section>

    <section id="about">
        <h2>Giới Thiệu</h2>
        
        <div class="personal-info">
            <p>I'm a student at **<a href="URL_TRUONG">Foreign Trade University</a>** in Ha Noi, Viet Nam.</p>
            <p>
                <a href="mailto:your_email@example.com">Email</a> / 
                <a href="URL_GITHUB">Github</a>
            </
